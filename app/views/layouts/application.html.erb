<!DOCTYPE html>
<html>
<head>
  <title> <%= institution_name -%> Authentication Service</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

  <%# A logo of sorts-%>
  <h1><%= institution_name -%></h1>

  <div class="flash">
    <% flash.each do |type, message| -%>
      <div class="message <%= type %>">
        <p><%= message %></p>
      </div>
    <% end -%>
  </div>

  <%= yield %>

</body>
</html>
